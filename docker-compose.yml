services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./output:/app/output
  # Point LLM_BASE_URL at a host Ollama/vLLM server or add a local model service here.